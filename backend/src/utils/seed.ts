import Agent from '../models/Agent';

export async function seedAgents() {
  // Dump data seeding has been disabled to prevent injecting mock records into production/development databases.
  console.log('Seed agents skipped: No dump data to seed.');
}
